services:
  - type: web
    name: ecommerce-analytics-dashboard
    env: python
    buildCommand: cd local-demo && pip install -r requirements.txt && python setup_render.py
    startCommand: cd local-demo && python dashboard.py
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0

